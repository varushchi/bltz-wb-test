version: '3.9'

services:
  app:
    build: .
    env_file:
      - .env
    depends_on:
      - my_postgres

  my_postgres:
    image: postgres:15
    restart: always
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  pgdata:
